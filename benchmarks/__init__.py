"""Benchmark suites, per the engineering brief section 26: a capability
layer (conversation, knowledge, reasoning — needs a trained model, not
present in this delivery) and a system layer (tool selection, schema
validity, grounding, latency — ``tools/`` has a real, runnable example
today; the rest need a trained model or a native runtime). See each
subdirectory's README for its specific status.
"""
